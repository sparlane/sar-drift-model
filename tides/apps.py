"""
App config for tides
"""
from django.apps import AppConfig


class TidesConfig(AppConfig):
    """
    Tides app config
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "tides"
